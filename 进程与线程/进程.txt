应用程序
最开始的时候，单任务的,一件一件的来
切换：切换需要再回来
进程本质就是一段程序运行的过程，抽象的概念，程序（怎么去做），数据集（资源，数据），进程控制块（怎么保存，怎么切换，怎么返回等）动态的过程
程序是程序，进程是进程

耗费资源


管理工具
纳秒级别

I/O切换
时间切换
一个cpu对几个任务，在cpu执行A任务的时候，当遇到I/O阻塞的时候，需要下载大量的数据，这时cpu是空闲的，闲着也是闲着，就可以切换到B任务，cpu执行B任务。等到A的I/O操作完成在切换回来


I/O阻塞   没有立即做出响应，资源等待