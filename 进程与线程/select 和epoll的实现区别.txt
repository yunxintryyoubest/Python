select在全部的系统都可以运行，wins下只有select

epoll不可以在wins运行
可以知道是谁提交数据的，epoll属于同步，伪异步

异步：一丁点的阻塞都不能存在


1.阻塞I/O（一直在等数据）
2.非阻塞I/O（一直在监听，非阻塞）
3.I/O多路复用，（多个窗口，I/O并发，复用）

4.异步I/O:效率最好的(不用一直等，可以不同时间，一点阻塞都可以没有，比如你在抢票，可以一直抢票，又做其他的事情）

异步最大特点：
全程无阻塞