缓存数据库（非关系型数据库）

提高访问数据

Nosql
not only sql
泛只非关系型数据库，随着互联网web2.0网站的兴起，传统的关系数据库在应付web2.0
网站，特别是超大规模和高并发的sns类型的web2.0纯动态网站已经显得力不从心
暴露了很多难以克服的问题，而非关系型数据库这对由于其本身的特点的得到了非常迅速的发展，nosql数据哭的
产生是为了解决大规模数据集合多重数据种类带来的挑战，尤其是大数据应用难题。

web1.0  只能看
web2.0  

sns  社会网络世界

social network society

四大分类：
键值：（key-Value）哈希算法，去重

文档型数据库：
json，键值对的升级版   mongodb

图形数据库：
graph




基于原子的操作，实现master-slave，安全的，数据都是保存在内存里
redis把数据写在磁盘，保证数据的持久化


事务：保证事情的一系列完整性


优点：速度非常快

redis支持最大多数开发人员已经知道如列表，集合，可排序集合，哈希等
数据类型，这使得在应用中很容易解决的各种问题，我们知道处理那些问题用那种数据类型可以更好解决

操作都是基于原子，操作都是原子，从而确保当两个用户同时访问redis服务端得到的是更新后
的值
multiutility工具：redis是一个多功能实用工具，看样子啊好多如：缓存，消息队列中使用（redis原生主持发布/订阅）
在web应用程序会话，网站页面点击等任何短暂的数据

单进程，单线程
I/O多路复用









sina数据存在缓存里面，缓存是存在内存里面
而其他的存在数据库的话，是存在硬盘里面，即使加了索引，
这个访问速度有点慢 


用户自产生内容



非关系型数据库

缓存数据库
列式存储
很多维度

文档型的数据库

graph

string list set zset hash

安全的原子性的，要么成功，要么失败

会周期性的根性数据到磁盘，持久化

单进程，单线程







